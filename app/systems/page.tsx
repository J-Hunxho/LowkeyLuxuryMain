import { Reveal } from '@/components/ui/animated';
const stacks = [{ name:'Acquisition Stack', chain:['Website','CRM logic','Telegram alerts','Operator dashboard'], note:'Captures demand, qualifies it, and routes it to the right person without delay.' },{ name:'Revenue Stack', chain:['Stripe','Offer logic','Automated fulfillment','Internal logging'], note:'Connects payment to delivery and keeps reporting visible without manual reconciliation.' },{ name:'Execution Stack', chain:['Knowledge base','AI agents','Approval layer','Client communication'], note:'Supports speed while preserving operator review, escalation logic, and quality control.' }];
/**
 * Render the Systems page that displays infrastructure stacks and their chain steps with animated reveal effects.
 *
 * @returns The React element for the Systems page layout.
 */
export default function SystemsPage() { return <div className="shell py-24"><Reveal className="max-w-3xl"><p className="eyebrow mb-4">Systems</p><h1 className="font-serif text-5xl sm:text-6xl">Infrastructure stacks that connect the business.</h1><p className="mt-6 text-lg leading-8 text-white/58">Telegram, Stripe, APIs, AI agents, and backend workflows arranged into one coherent operating system.</p></Reveal><div className="mt-16 space-y-8">{stacks.map((stack, index) => <Reveal key={stack.name} delay={index * 0.08} className="panel p-8 lg:p-10"><div className="grid gap-10 lg:grid-cols-[0.9fr_1.1fr]"><div><p className="text-sm uppercase tracking-luxe text-gold-300/70">Stack 0{index + 1}</p><h2 className="mt-5 font-serif text-4xl">{stack.name}</h2><p className="mt-4 max-w-lg leading-7 text-white/58">{stack.note}</p></div><div className="rounded-[1.75rem] border border-white/8 bg-black/25 p-6"><div className="grid gap-4 md:grid-cols-4">{stack.chain.map((item, itemIndex) => <div key={item} className="relative rounded-[1.25rem] border border-gold-400/10 bg-white/[0.03] p-4 text-center"><p className="text-sm text-white/70">{item}</p>{itemIndex < stack.chain.length - 1 ? <span className="absolute -right-3 top-1/2 hidden h-px w-6 bg-gold-400/40 md:block" /> : null}</div>)}</div></div></div></Reveal>)}</div></div>; }
