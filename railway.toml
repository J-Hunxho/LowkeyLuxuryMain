[build]
builder = "NIXPACKS"
buildCommand = "npm ci --include=dev && npm run railway:preflight && npm run build"

[deploy]
startCommand = "next start -H 0.0.0.0 -p ${PORT:-3000}"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
